========
A struct
========

.. doxy:c:: ipv6_hdr_t
    :children:

    IPv6 rocks!!!!

.. doxy:c:: sx127x_t
    :children:

.. doxy:c:: struct[cortexm_base_t]
    :children:

    Members starting with "_" were removed.

.. doxy:c:: struct[ringbuffer_t]
    :children:

    Inside a struct we can refer to a members using a syntax that would normally
    be ambiguous: :doxy:r:`[size]`
